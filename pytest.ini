[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
# Block ROS plugins from interfering (entry point names as registered in pytest11)
addopts = -p no:launch_pytest -p no:launch_testing -p no:launch_ros
